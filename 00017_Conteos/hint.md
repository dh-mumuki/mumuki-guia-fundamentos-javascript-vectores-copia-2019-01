A una lista le podiamos pedir el tamaño con el`length`.
Ejemplo:

````javascript
var lista = ["Juan","Pepe"];
var cantidadElementosLista = lista.length;
console.log(cantidadElementosLista) //devuelve 2
````